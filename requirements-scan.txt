# HexStrike AI — dependency MINIMAL untuk image scan CI/CD (pre & post deploy).
# Dipakai bersama oleh Dockerfile.predeploy & Dockerfile.postdeploy.
# Hanya yang di-import saat hexstrike_server.py dimuat + webhook + client OpenAI.
# TIDAK termasuk pwntools/angr (hanya di-import lazy di fungsi binary/CTF,
# tidak dipakai pada tahap code-scan/pentest).

flask>=2.3.0,<4.0.0
requests>=2.31.0,<3.0.0
psutil>=5.9.0,<6.0.0
aiohttp>=3.8.0,<4.0.0
beautifulsoup4>=4.12.0,<5.0.0
selenium>=4.15.0,<5.0.0
webdriver-manager>=4.0.0,<5.0.0
mitmproxy>=9.0.0,<11.0.0

# Ringkasan AI opsional (--use-llm) via vLLM
openai>=1.30.0,<2.0.0
